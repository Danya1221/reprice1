name: Apply unified Apple grouping

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-apple-unified-grouping.yml
      - apply_apple_unified_grouping.py
      - apply_apple_unified_grouping_v2.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply Apple grouping fix
        run: python apply_apple_unified_grouping_v2.py
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: python -m unittest discover -s tests -v
      - name: Commit fix
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add prices.py tests/test_prices.py
          git diff --cached --check
          git commit -m "Unify Apple price sections and parse MacBook shorthand"
          git pull --rebase origin main
          git push origin HEAD:main
