name: Apply compact iPhone catalog

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-compact-iphone-catalog.yml
      - apply_compact_iphone_catalog.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply compact catalog fix
        run: python apply_compact_iphone_catalog.py
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run tests
        run: python -m unittest discover -s tests -v
      - name: Commit fix
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add catalog_publisher.py tests/test_catalog.py
          git diff --cached --check
          git commit -m "Use three compact iPhone catalog buttons"
          git pull --rebase origin main
          git push origin HEAD:main
